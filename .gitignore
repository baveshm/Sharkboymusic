# Secrets — never commit
.env

# Traefik ACME certs — regenerated by Traefik on deploy
traefik/acme/*.json

# Raw uploaded assets (pre-processed / source material)
Xav.zip
Xav_extracted/
IMG_1696.JPG
/sharkboydraft1.mp4
Sharkboy Corporate Portfolio.pdf
Sharkboy Media Kit.pdf

# Local-only documentation (infra-specific, not for the repo)
DEPLOY.md
main-traefik-sni-passthrough.yml

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
